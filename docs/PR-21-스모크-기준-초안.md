# PR-21: 스모크 기준(초안)

## 대상
- 외부기관 연계 API: `/api/external-sync`

## 입력
- 기본 요청 본문에 `from`, `to`(YYYY-MM-DD)를 포함한다.
- 실제 외부기관 API 스펙과 무관하게 내부 기준으로만 사용한다.

## PASS 기준
- HTTP 200 응답
- 응답 본문에 `requestId` 포함
