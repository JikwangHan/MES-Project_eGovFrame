# PR-12: 검증 규칙/임계치(초안)

## 목적
- 자동 식별/정규화 전에 최소 검증을 통과할 수 있는 기준을 정의한다.

## 기본 규칙(초안)
- payload 길이 1자 이상
- JSON 형식일 경우 파싱 가능해야 함
- confidence 0.5 미만은 기본 격리

## 임계치(초안)
- confidence >= 0.7: 자동 승인
- 0.5 <= confidence < 0.7: 보류(검토)
- confidence < 0.5: 격리

## 비고
- 임계치는 운영 단계에서 조정 가능
