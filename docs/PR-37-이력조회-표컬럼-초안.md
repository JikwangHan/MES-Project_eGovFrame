# PR-37: 연계 이력 조회 UI 표 컬럼 정의(초안)

## 목적
- 연계 이력 조회 화면에서 표시할 기본 컬럼을 정의한다.

## 컬럼(초안)
- 요청시간(time)
- 요청ID(requestId)
- 회사ID(companyId)
- 기간 시작(from)
- 기간 종료(to)
- 상태(status)
- 접수시간(acceptedAt)
- 오류코드(errorCode)
- 오류메시지(errorMessage)

## 비고
- 표시 순서는 UX 상황에 따라 조정 가능
