# PR-11: 스모크 기준(초안)

## PASS 기준
- (수신) 원시 데이터 1건 이상이 파이프라인에 투입된다.
- (분류) 분류 결과가 1건 이상 기록된다.
- (정규화) 정규화 데이터가 1건 이상 저장된다.
- (표준 로그) [PASS] 라인으로 처리 결과가 출력된다.

## FAIL 기준
- 파이프라인 기동 실패
- 분류/정규화 단계 오류

## SKIP 기준
- 스키마/보관 정책 미확정 시 [SKIP]

## 스모크 스크립트(예정)
- scripts/smoke-ai-normalize.ps1
- scripts/smoke-ai-normalize.sh

## 최근 실행 결과
- [PASS] ai normalize smoke
- [PASS] ai normalize smoke (재검증)
