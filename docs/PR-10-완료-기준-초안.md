# PR-10: 완료 기준(초안)

## 완료 기준(초안)
- 미들웨어 제어 신호 송신 API가 기동된다.
- 제어 요청 1건 이상을 송신 처리한다(실장비 미확정 시 모의 송신 포함).
- 송신 결과가 표준 로그([PASS]/[FAIL]/[SKIP])로 남는다.
- 스모크(또는 테스트)로 최소 송신이 확인된다.
- 문서에 제어 신호 포맷/흐름이 정리된다(민감정보 제외).

## 완료 근거
- [PASS] control signal smoke

## 필요 확인
- 제어 명령 종류/우선순위 확정
