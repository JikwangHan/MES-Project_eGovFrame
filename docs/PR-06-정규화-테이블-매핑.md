# PR-06: 정규화 모델 ↔ 테이블 매핑(초안)

## 매핑 규칙
- companyId -> telemetry.company_id
- deviceId -> telemetry.device_id
- timestamp -> telemetry.timestamp
- metricKey -> telemetry.metric_key
- metricValue -> telemetry.metric_value
- unit -> telemetry.unit
- rawId -> telemetry.raw_id

## 비고
- 변경 가능하며, 확정 전까지 수정 가능하다.
