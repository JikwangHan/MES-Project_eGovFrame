# PR-08: 스모크 기준(초안)

## PASS 기준
- GET /health 200
- GET /api/dashboard/summary 200
- GET /api/equipments 200
- GET /api/orders 200
- GET /api/jobs 200
- GET /api/kpi 200
- GET /api/kpi/trend 200
- GET /ui/dashboard/production 200
- GET /ui/equipment/status 200
- GET /ui/orders 200
- GET /ui/jobs 200
- GET /ui/kpi 200
- (조건부) 설비 목록에 deviceId가 있으면 /api/equipments/{deviceId}/telemetry 200
- (표시 확인) /ui/orders, /ui/jobs, /ui/kpi 화면에서 "데이터 로딩 중..." 문구가 사라지고 테이블이 채워진다.
- (표시 확인) /ui/orders, /ui/jobs, /ui/kpi 화면에 필터 입력 폼이 표시된다.
- (동작 확인) 필터 입력 후 조회 버튼을 누르면 API 쿼리 파라미터가 반영된 데이터가 표시된다.
- (동작 확인) 예: /ui/orders에서 상태=DONE을 입력하면 ORD-002만 표시된다.
- (표시 확인) /ui/orders, /ui/jobs, /ui/kpi 화면에 입력 형식 안내 문구가 표시된다.
- (경고 확인) 잘못된 날짜 형식 입력 시 경고 문구가 표시된다.
- (API 확인) 잘못된 날짜/상태 값을 직접 호출하면 400 응답이 발생한다.
- (표시 확인) API 400 응답 시 화면에 오류 메시지가 표시된다.
- (표시 확인) 오류 메시지에 에러코드(E-1001/E-1002)가 함께 표시된다.
- (동작 확인) 잘못된 값 입력 시 조회 버튼이 비활성화된다.
- (표시 확인) 조회 버튼 비활성화 시 안내/보조 설명이 표시된다.
- (경고 확인) 기간 시작이 종료보다 늦으면 경고 문구가 표시된다.
- (경고 확인) 유효하지 않은 날짜(월/일 범위) 입력 시 경고 문구가 표시된다.
- (API 확인) 유효하지 않은 날짜(월/일 범위)로 호출하면 400 응답이 발생한다.
- (API 확인) 기간 시작이 종료보다 늦으면 400 응답이 발생한다.
- (표시 확인) 에러코드별 한글 메시지가 표시된다.
- (표시 확인) 경고 문구에 대상 필드명이 포함된다(예: 납기일/작업 기간/KPI 기간).
- (표시 확인) 잘못된 입력값이 있는 필드에 붉은 테두리 강조가 표시된다.
- (동작 확인) 잘못된 값을 정상 값으로 수정하면 경고/테두리가 제거되고 버튼이 활성화된다.
- (표시 확인) 모든 필터가 비어 있으면 전체 조회 안내 문구가 표시된다.
- (표시 확인) 최근 조회 조건 요약이 표시된다.
- (표시 확인) 최근 조회 조건 요약은 모든 입력값이 비어 있을 때만 "전체"로 표시된다.
- (표시 확인) 최근 조회 조건 요약은 12자 초과 시 축약 표시된다.
- (표시 확인) 최근 조회 조건 요약은 사용자 친화적 라벨로 표시된다.
- (표시 확인) 최근 조회 조건 요약의 항목 순서가 고정된다.
- (표시 확인) 최근 조회 조건 요약에서 공백/특수문자가 정리된다.
- (표시 확인) 최근 조회 조건 요약은 최대 4개 항목만 표시된다.
- (표시 확인) 4개 초과 시 "외 n건"이 함께 표시된다.

## SKIP 기준
- MES Web 서버 미기동 시 [SKIP]

## 스모크 스크립트
- scripts/smoke-ui-binding.ps1
- scripts/smoke-ui-binding.sh

## 최근 실행 결과
- [PASS] ui binding smoke
- [PASS] ui binding smoke (재검증)
