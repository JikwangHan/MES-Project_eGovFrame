# PR-17: DB 에러 코드 규칙(초안)

## 목적
- DB 연동 실패를 사용자에게 일관된 코드로 전달한다.

## 기본 원칙
- 내부 예외 메시지는 외부로 노출하지 않는다.
- 공통 응답 포맷(`result`, `message`, `errorCode`, `data`)을 유지한다.

## 에러 코드(초안)
- DB-0001: DB 연결 실패
- DB-0002: DB 쿼리 실패

## 적용 방식(현 단계)
- 현재는 프레임워크 기본 500 응답이 발생할 수 있다.
- PR-17에서 에러코드 매핑을 단계적으로 적용한다.
